ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
            base:
              content_attachment_byte_size_is_too_big: 添付ファイルは%{max_content_attachment_mega_byte_size}MG以下にしてください[%{bytes}/%{max_bytes}]
            title:
              blank: が空ですよ
              too_long: が文字数が%{count}文字を超過しています
            content:
              too_long: 文字数が%{max_content_length}文字超過しています。[%{length}文字/%{max_content_length}文字]
    attributes:
      post:
        title: タイトル
        content: コンテンツ
